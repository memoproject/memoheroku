package servlet;

import javax.xml.ws.Endpoint;

import myServ.MyServ;


//for heroku
public class NotUsedExporter {
	
/*	public static void main(String[] args) {
		
		String address = "http://0.0.0.0:"+RunServlets.port+"/";
		//LOGGER.log(Level.INFO, "Starting jetty on port: {0}", port);
		System.err.println("porttinro: " + RunServlets.port);
		Endpoint.publish(address, new MyServ());
		//Endpoint.publish("http://localhost:8080/MyServices/pservice", new MyServ());
	}*/
}